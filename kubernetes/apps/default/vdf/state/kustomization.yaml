---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
# Intentionally empty: the vdf-state Kustomization in ../ks.yaml adds only
# components/kopiur/backup here, for vdf's second volume.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
