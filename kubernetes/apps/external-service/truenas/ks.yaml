---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: truenas
spec:
  interval: 1h
  path: ./kubernetes/apps/external-service/truenas/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: external-service
  wait: true
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
